from setuptools import setup

setup(
   name='evolution',
   version='0.1',
   description='Simulation of the evolution',
   author='Me',
   packages=['evolution'],
   install_requires=['random', 'pprint'],
